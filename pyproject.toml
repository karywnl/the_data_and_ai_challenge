[project]
name = "fitsignal"
version = "0.1.0"
description = "FitSignal — intelligent candidate discovery & ranking for the Redrob Senior AI Engineer JD"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "sentence-transformers>=3.0",
]

[tool.uv]
package = false
