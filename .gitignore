# --- Provided dataset (large; distributed separately) ---
data/
*.jsonl
*.jsonl.gz
# ...but keep the small demo sample shipped with the sandbox
!sandbox/sample_candidates.jsonl

# --- Regenerable precomputed artifacts (rebuilt by precompute.py) ---
artifacts/

# --- Generated ranking outputs (reproduced from rank.py) ---
submission.csv
*_test.csv
explore_report.json
# Team upload copies (the repo keeps source + reproduce command, not the outputs)
Alonecoder.csv
Alonecoder.xlsx

# --- Python / uv ---
.venv/
__pycache__/
*.py[cod]
.uv/
.ruff_cache/
.pytest_cache/

# --- Editors / OS ---
.DS_Store
Thumbs.db
.idea/
.vscode/

# --- Local machine settings (keep shared Claude guidance) ---
.claude/settings.local.json
